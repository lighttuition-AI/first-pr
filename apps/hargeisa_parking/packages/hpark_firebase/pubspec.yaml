name: hpark_firebase
description: "Firebase Auth + Firestore implementations of the HPark backend seams (OfficerRepository, AuthService)."
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.12.1
  flutter: ">=3.44.0"

dependencies:
  cloud_firestore: ^6.5.0
  firebase_auth: ^6.5.2
  firebase_core: ^4.10.0
  flutter:
    sdk: flutter
  hpark_core:
    path: ../hpark_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
