name: hpark_core
description: "Hargeisa Parking shared core — design system, models, and data repositories for the HPark app ecosystem."
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.12.1
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
